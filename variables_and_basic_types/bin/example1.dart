void main(List<String> args) {
  const name = "Foo" ; 
  print(name);
  //invalid code
  //name = "Boo"

  //const avoid assigning new value to variable and modifying existing value.

  //const age = [1,2,3,4];
  //print(age);
  //invalid code because we can't modify internal value of constant variable.
  //age.removeAt(0);
}