void main(List<String> args) {
  final double age  = 30;  // promoting int to double
  // final int age = 30.5;  // we cant demote it, that is double to int. because double is having high precision.
  print(age);
}